<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mju.edu.DAO.ResourceDao" >
    <select id="getResByPath" resultType="int">
        select count(*) from t_resource where path=#{path}
    </select>
</mapper>